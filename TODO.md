* Implement some commands:  @desc, @create, @dig, @tunnel, @attr, @flag, etc.

* Add perms/locks to Zobjects and make @name permission sensitive (and other similar commands)

* Implement .z file load on startup
* externalize the HTML rendering to settings (stuff like the nbsps and brs as well as CSS)
