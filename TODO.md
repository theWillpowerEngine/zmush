* Objects:
  * drop and pickup and various permutations around that
  * Add to room description frame
  * Custom commands
    * Which also requires attributes

* Exits
  * @cr/ex
  * Locks
  * Reality Levels (plumb into everywhere), or equivalent of DARK

* note:  When @creating things we have to find the lowest number that isn't taken, not just auto increment

* Implement .z file load on startup
* externalize the HTML rendering to settings (stuff like the nbsps and brs as well as CSS)

